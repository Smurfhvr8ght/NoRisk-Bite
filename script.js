function handleLogin(event) {
    event.preventDefault();

    // Simulate successful login (you can add validation here later)
    // Redirect to HomePage.html
    window.location.href = "HomePage.html";
}
